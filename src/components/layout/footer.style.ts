import { css } from 'lit';

export const footerStyles = css`
  :host {
    display: block;
  }

  .nr-footer {
    padding: 1rem 1.5rem;
    background: #ffffff;
    color: #161616;
    border-top: 1px solid #e0e0e0;
    font-size: 0.875rem;
    line-height: 1.5;
    transition: all 0.2s;
  }
`;
